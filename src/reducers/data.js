

const data = (state, action) => {
    switch (action.type) {
        case 'SEARCH_VIDEO':
            return state
            break;
    
        default:
              return state  
            break;
    }

}

export default data